<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.codicefun.blog.mapper.CommentMapper">

  <insert id="insert">
    INSERT INTO `comment` (`nickname`, `email`, `content`, `blog_id`, `parent_id`, `liked`)
    VALUES (#{nickname}, #{email}, #{content}, #{blogId}, #{parentId}, #{liked})
  </insert>

  <select id="selectById" resultType="comment">
    SELECT *
    FROM `comment`
    WHERE `id` = #{id}
  </select>

  <select id="selectAll" resultType="comment">
    SELECT *
    FROM `comment`
  </select>

  <update id="update">
    UPDATE `comment`
    <set>
      <if test="nickname != null and nickname != ''">
        `nickname` = #{nickname},
      </if>
      <if test="email != null and email != ''">
        `email` = #{email},
      </if>
      <if test="content != null and content != ''">
        `content` = #{content},
      </if>
      <if test="blogId != null">
        `blog_id` = #{blogId},
      </if>
      <if test="parentId != null">
        `parent_id` = #{parentId},
      </if>
      <if test="liked != null">
        `liked` = #{liked},
      </if>
    </set>
    WHERE `id` = #{id}
  </update>

  <delete id="delete">
    DELETE
    FROM `comment`
    WHERE `id` = #{id}
  </delete>

</mapper>

